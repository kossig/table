/*function getWindowSize(){*/
	//let bp,
	//lg = 1170,
	//md = 960,
	//sm = 780,
	//xs = 560,
	//html = document.querySelector('#windowSize'),
	//p1 = document.createElement('p'),
	//p2 = document.createElement('p');
	//html.appendChild(p1);
	//html.appendChild(p2);

	//function calcWidth(){
		//let	width = window.innerWidth;

		//[> On screens that are 992px or less, set the color to blue <]
		//if ( lg < width ){
			//bp = '';
		//} else if ( lg >= width && width > md ) {
			//bp = `width less lg = ${lg} tan`;
		//} else if ( md >= width && width > sm ) {
			//bp = `width less md = ${md} pink`;
		//} else if ( sm >= width && width > xs ) {
			//bp = `width less sm = ${sm} yellow`; 
		//} else if ( width < xs ) {
			//bp = `width less xs = ${xs} orange`;
		//}

		//p1.innerHTML = width + ' × ' + window.innerHeight;
		//p2.innerHTML = bp;
	//}

	//window.addEventListener("resize", calcWidth);

	//calcWidth();
/*}*/
